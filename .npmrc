# Use hoisted node_modules for React Native compatibility
node-linker=hoisted

# Enforce engine requirements (fails on wrong Node/pnpm version)
engine-strict=true

# Save exact versions by default
save-exact=true

# Auto-install peer dependencies
auto-install-peers=true
